<div class="container">
  <div class="row">
    <div class="col-md-12 text-center">
      <h1>Scrape The News</h1>
    </div>
  </div>
  <div class= "row">
    <div class="col-md-12 text-center">
      <button id="scrapeESPN" class= "btn btn-primary">Scrape Sports News</button>
    </div>
  </div>
  <div class= "row">
    <div class="col-md-12">
      <div id="notes"></div>
      <div id="prevNotes"></div>
      {{#each newsScrape}}
        <h3 data-id= "{{this._id}}">{{this.articleNumber}}. {{this.date}}- {{this.title}}</h3>
        <a href="http://www.espn.com/{{this.link}}">{{this.link}}</a>
         <div id="notes"></div>
         <div id="prevNotes"></div>
      {{/each}}
    </div>
  </div>
</div>

